import './Form.css';
import { useState } from "react";
import { Col, Form, Row, FormControl, Button } from "react-bootstrap";
import { ICompany } from '../../../models/ICompany';


const { Group, Label, Control, Check, Select } = Form;

interface IEditCompanyFormProps {
    company?: ICompany
}

const EditCompanyForm = ({ company }: IEditCompanyFormProps) => {
    const handleSubmit = (e: any) => {
        e.preventDefault();
        console.log(e.target.comapnyId.value);
    }

    return (
        <Row>
            <Col sm={12}>
                <div className="container-form">
                    <Form onSubmit={handleSubmit}>
                        <Group>
                            <span className="input-block">
                                <Label>Id</Label>
                                <Control type="number" className="coupon-edit-input" name="id" defaultValue={company ? company.id.toString() : "id"} disabled={company ? true : false} />
                            </span>
                        </Group>
                        <Group>
                            <span className="input-block">
                                <Label>Address</Label>
                                <Control type="text" className="coupon-edit-input" defaultValue={company ? company.address.toString() : "address"} />
                            </span>
                        </Group>
                        <Group>
                            <span className="input-block">
                                <Label>Name</Label>
                                <Control type="text" className="coupon-edit-input" defaultValue={company ? company.name.toString() : "name"} />
                            </span>
                        </Group>
                        <Group>
                            <span className="input-block">
                                <Label>Phone Number</Label>
                                <Control type="text" className="coupon-edit-input" defaultValue={company ? company.phoneNumber.toString() : "phoneNumber"} />
                            </span>
                        </Group>
                        <Group>
                            <span className="input-block">
                                <Label>Company Image</Label>
                                <Control type="text" className="coupon-edit-input" defaultValue={company ? company.companyImage.toString() : "companyImage"} disabled={company ? true : false} />
                            </span>
                        </Group>
                        <Button type="submit" className="btn-submit-edit">submit</Button>
                        {/* <div className='error-reg'>
                            {error !== '' && <h6> {error} </h6>}
                        </div> */}
                    </Form>
                </div>
            </Col>
        </Row >
    );
}

export default EditCompanyForm;